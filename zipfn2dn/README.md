#zipfn2dn

指定したzipファイルの名前を、それに含まれるディレクトリの名前にコピーする

##例

```
 zipfile.zip
   dir/file1
   dir/file2
```

こういう zip ファイルを


```
 zipfile.zip
   zipfile/file1
   zipfile/file2
```

という構造に変更する
 